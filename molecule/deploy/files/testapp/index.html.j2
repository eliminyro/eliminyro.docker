<!DOCTYPE html>
<html>
<head>
    <title>Test Application - {{ ansible_hostname }}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .container { max-width: 800px; margin: 0 auto; }
        .info { background: #f5f5f5; padding: 20px; margin: 20px 0; border-radius: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Test Application Deployed Successfully</h1>
        <div class="info">
            <h2>Deployment Information</h2>
            <p><strong>Host:</strong> {{ ansible_hostname }}</p>
            <p><strong>Date:</strong> {{ ansible_date_time.iso8601 }}</p>
            <p><strong>Application:</strong> {{ playbook_app | default('Unknown') }}</p>
            <p><strong>Environment Variable:</strong> {{ testapp_env.TEST_ENV_VAR | default('Not set') }}</p>
        </div>
        <div class="info">
            <h2>Container Information</h2>
            <p><strong>Image:</strong> {{ testapp_image }}:{{ testapp_image_tag }}</p>
            <p><strong>Ports:</strong> {{ testapp_ports | join(', ') }}</p>
            <p><strong>Network:</strong> {{ testapp_networks | join(', ') | default('default') }}</p>
        </div>
        <div class="info">
            <h2>Dependencies</h2>
            <ul>
            {% for dep in testapp_deps %}
                <li>{{ dep.name }} ({{ dep.image }}:{{ dep.tag }})</li>
            {% endfor %}
            </ul>
        </div>
    </div>
</body>
</html>
