---
deploy_deps: "{{ lookup('vars', playbook_app + '_deps') }}"

deploy_configlist: "{{ lookup('vars', playbook_app + '_configlist') }}"
deploy_templatelist: "{{ lookup('vars', playbook_app + '_templatelist') }}"

deploy_image: "{{ lookup('vars', playbook_app + '_image') }}"
deploy_image_tag: "{{ lookup('vars', playbook_app + '_image_tag') }}"
deploy_command: "{{ lookup('vars', playbook_app + '_command') }}"
deploy_restart_policy: "{{ var[playbook_app + '_restart_policy'] }}"
deploy_networks: "{{ lookup('vars', playbook_app + '_networks') }}"
deploy_network_mode: "{{ lookup('vars', playbook_app + '_network_mode') }}"
deploy_capabilities: "{{ lookup('vars', playbook_app + '_capabilities') }}"
deploy_security_opt: "{{ lookup('vars', playbook_app + '_security_opt') }}"
deploy_privileged: "{{ lookup('vars', playbook_app + '_privileged') }}"
deploy_user: "{{ lookup('vars', playbook_app + '_user') }}"
deploy_hostname: "{{ lookup('vars', playbook_app + '_hostname') }}"
deploy_ports: "{{ lookup('vars', playbook_app + '_ports') }}"
deploy_env: "{{ lookup('vars', playbook_app + '_env') }}"
deploy_devices: "{{ lookup('vars', playbook_app + '_devices') }}"
deploy_volumes: "{{ lookup('vars', playbook_app + '_volumes') }}"
deploy_pull: "{{ lookup('vars', playbook_app + '_pull') }}"
deploy_healthcheck: "{{ lookup('vars', playbook_app + '_healthcheck') }}"
deploy_auto_remove: "{{ lookup('vars', playbook_app + '_auto_remove') }}"
deploy_recreate: "{{ lookup('vars', playbook_app + '_recreate') }}"
deploy_finish: "{{ lookup('vars', playbook_app + '_finish') }}"
